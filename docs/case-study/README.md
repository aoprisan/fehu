# Case study

Cross-project write-ups about fehu in use.

| File | What it is | Published |
|---|---|---|
| `two-games-one-ledger.html` | fehu across two unrelated games — DeadCity (Rust/Actix multiplayer shooter) and Global Conflict v2 (client-only browser PWA): the same missing conservation in two shapes, where the two integrations diverge, and the five things that generalised across both | https://claude.ai/code/artifact/2bdac111-4f5c-4a15-b653-d91afece5e40 |

The per-game design notes live in those repositories under `docs/fehu/`.

This file is an Artifact page source: content only, no `<!doctype>`/`<head>`/`<body>`
wrapper — those are supplied at publish time.

Nothing here is built. Neither integration exists yet; the documents are designs
written against the current code in each repository.
